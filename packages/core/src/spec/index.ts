/**
 * 公文版式规范（GB/T 9704-2012）—— 纯数据，零依赖。
 *
 * 该子模块不依赖 Tiptap / ProseMirror，可单独用于：版式计算、docx 导出、
 * 服务端校验、或其它框架的渲染器。
 */
export * from "./layout";
export * from "./font-size";
export * from "./fonts";
export * from "./elements";
